# OSI Cards CI/CD Pipeline
# 
# NOTE: This workflow is disabled. Use deploy.yml for Firebase deployments.
# Re-enable when full CI/CD pipeline is needed.

name: OSI Cards CI/CD Pipeline (Disabled)

on:
  workflow_dispatch:
  # Disabled automatic triggers - use deploy.yml instead
  # push:
  #   branches: [main, develop]
  # pull_request:
  #   branches: [main]

jobs:
  placeholder:
    name: CI/CD Disabled
    runs-on: ubuntu-latest
    if: false  # Always skip
    steps:
      - run: echo "This workflow is disabled. Use deploy.yml for deployments."
